---
import Picture from '@components/modules/Picture.astro';
import Svg from '@components/modules/Svg.astro';

export interface Props {
  gh: string;
}

const { gh } = Astro.props;
---

<header class='l_header'>
  <div class='l_header_logo'>
    <a href='https://jws.work/' class='u_flash'>
      <span class='u_flash_item'>
        <Svg svgId='logo' pcw={88} pch={24} spw={65} sph={17.5} alt='JWS' />
      </span>
    </a>
  </div>
  <div class='l_header_gh'>
    <a class='u_flash' href={gh} target='_blank' rel='noopener noreferrer'>
      <span class='u_flash_item'>
        <Svg
          svgId={`logo_gh`}
          pcw={26}
          pch={26}
          spw={29}
          sph={29}
          alt='GitHub'
        />
      </span>
    </a>
  </div>
</header>
